<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.4">Jekyll</generator><link href="https://wenbinwang12.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://wenbinwang12.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-04-07T11:01:27+00:00</updated><id>https://wenbinwang12.github.io/feed.xml</id><title type="html">blank</title><subtitle>A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Ensemble Learning</title><link href="https://wenbinwang12.github.io/blog/2025/distill/" rel="alternate" type="text/html" title="Ensemble Learning"/><published>2025-06-13T00:00:00+00:00</published><updated>2025-06-13T00:00:00+00:00</updated><id>https://wenbinwang12.github.io/blog/2025/distill</id><content type="html" xml:base="https://wenbinwang12.github.io/blog/2025/distill/"><![CDATA[<p><strong>“Two heads are better than one.”</strong></p> <p>In the field of machine learning, one of the most effective ways to improve the accuracy of predictive models is through ensemble learning.</p> <p><a href="https://wenbinwang12.github.io/assets/pdf/Midterm_(MIT_Summer_Intern).pdf" target="_blank">Midterm_PPT</a></p> <h2 id="introduction-what-is-ensemble-learning">Introduction: What is Ensemble Learning?</h2> <p>Ensemble learning is a machine learning technique that combines multiple models to create a more accurate and robust model than any individual model could achieve on its own. It leverages multiple machine learning algorithms collectively to address classification or regression problems. This is done by training several models on the same/independent dataset and then aggregating their predictions to make a final decision. The algorithms used can either be of the same type (<strong>homogeneous ensemble learning</strong>) or different types (<strong>heterogeneous ensemble learning</strong>).</p> <h2 id="why-do-we-need-ensemble-learning">Why Do We Need Ensemble Learning?</h2> <p>Ensemble learning offers numerous advantages. Here, we’ll examine its most compelling benefits and explain why it’s such a powerful tool.</p> <ul> <li><strong>Reducing Overfitting:</strong> One of the main benefits of ensemble learning is that it can help to reduce overfitting. Overfitting occurs when a model is too complex and fits the training data too closely, resulting in poor generalization to new data. Ensemble learning can help to reduce overfitting by combining multiple models that have been trained on different subsets of the data or with different algorithms. This helps to reduce the variance of individual models and improve the generalization performance of the ensemble.</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">sklearn.ensemble</span> <span class="kn">import</span> <span class="n">BaggingClassifier</span>
<span class="kn">from</span> <span class="n">sklearn.tree</span> <span class="kn">import</span> <span class="n">DecisionTreeClassifier</span>

<span class="c1"># Create a decision tree classifier
</span><span class="n">tree</span> <span class="o">=</span> <span class="nc">DecisionTreeClassifier</span><span class="p">()</span>

<span class="c1"># Create a bagging classifier
</span><span class="n">bag</span> <span class="o">=</span> <span class="nc">BaggingClassifier</span><span class="p">(</span><span class="n">base_estimator</span><span class="o">=</span><span class="n">tree</span><span class="p">,</span> <span class="n">n_estimators</span><span class="o">=</span><span class="mi">10</span><span class="p">,</span> <span class="n">random_state</span><span class="o">=</span><span class="mi">42</span><span class="p">)</span>

<span class="c1"># Train the bagging classifier
</span><span class="n">bag</span><span class="p">.</span><span class="nf">fit</span><span class="p">(</span><span class="n">X_train</span><span class="p">,</span> <span class="n">y_train</span><span class="p">)</span>

<span class="c1"># Make predictions on the test data
</span><span class="n">y_pred</span> <span class="o">=</span> <span class="n">bag</span><span class="p">.</span><span class="nf">predict</span><span class="p">(</span><span class="n">X_test</span><span class="p">)</span>
</code></pre></div></div> <p>In this example, we create a decision tree classifier and use it as the base estimator for the bagging classifier. We set the number of estimators to 10, which means we will train 10 models on different subsets of the data.</p> <ul> <li><strong>Improving Accuracy:</strong> Ensemble learning can also help to improve the accuracy of models. By combining multiple models, we can take advantage of their individual strengths and weaknesses. For example, one model may be good at capturing linear relationships in the data, while another model may be good at capturing non-linear relationships. By combining these models, we can create a more accurate model that takes advantage of the strengths of each individual model.</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">sklearn.ensemble</span> <span class="kn">import</span> <span class="n">VotingClassifier</span>
<span class="kn">from</span> <span class="n">sklearn.linear_model</span> <span class="kn">import</span> <span class="n">LogisticRegression</span>
<span class="kn">from</span> <span class="n">sklearn.neighbors</span> <span class="kn">import</span> <span class="n">KNeighborsClassifier</span>
<span class="kn">from</span> <span class="n">sklearn.tree</span> <span class="kn">import</span> <span class="n">DecisionTreeClassifier</span>

<span class="c1"># Create the individual classifiers
</span><span class="n">lr</span> <span class="o">=</span> <span class="nc">LogisticRegression</span><span class="p">(</span><span class="n">random_state</span><span class="o">=</span><span class="mi">42</span><span class="p">)</span>
<span class="n">knn</span> <span class="o">=</span> <span class="nc">KNeighborsClassifier</span><span class="p">()</span>
<span class="n">tree</span> <span class="o">=</span> <span class="nc">DecisionTreeClassifier</span><span class="p">(</span><span class="n">random_state</span><span class="o">=</span><span class="mi">42</span><span class="p">)</span>

<span class="c1"># Create the ensemble classifier
</span><span class="n">ensemble</span> <span class="o">=</span> <span class="nc">VotingClassifier</span><span class="p">(</span><span class="n">estimators</span><span class="o">=</span><span class="p">[(</span><span class="sh">'</span><span class="s">lr</span><span class="sh">'</span><span class="p">,</span> <span class="n">lr</span><span class="p">),</span> <span class="p">(</span><span class="sh">'</span><span class="s">knn</span><span class="sh">'</span><span class="p">,</span> <span class="n">knn</span><span class="p">),</span> <span class="p">(</span><span class="sh">'</span><span class="s">tree</span><span class="sh">'</span><span class="p">,</span> <span class="n">tree</span><span class="p">)],</span> <span class="n">voting</span><span class="o">=</span><span class="sh">'</span><span class="s">hard</span><span class="sh">'</span><span class="p">)</span>

<span class="c1"># Train the ensemble classifier
</span><span class="n">ensemble</span><span class="p">.</span><span class="nf">fit</span><span class="p">(</span><span class="n">X_train</span><span class="p">,</span> <span class="n">y_train</span><span class="p">)</span>

<span class="c1"># Make predictions on the test data
</span><span class="n">y_pred</span> <span class="o">=</span> <span class="n">ensemble</span><span class="p">.</span><span class="nf">predict</span><span class="p">(</span><span class="n">X_test</span><span class="p">)</span>
</code></pre></div></div> <p>In this example, we create three different classifiers (logistic regression, k-nearest neighbors, and decision tree) and use them as the base estimators for the voting classifier. We set the voting parameter to ‘hard’, which means that the predictions are based on a simple majority vote.</p> <ul> <li><strong>Handling Noisy Data (Increased Stability and Robustness):</strong> Ensemble learning can also be useful when working with noisy data. Noisy data can make it difficult to train accurate models, as the noise can introduce errors and biases into the model. Ensemble learning can help to reduce the impact of noise by combining multiple models that have been trained on different subsets of the data or with different algorithms. This helps to reduce the impact of noise on individual models and improve the overall accuracy of the ensemble.</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">sklearn.ensemble</span> <span class="kn">import</span> <span class="n">VotingClassifier</span>
<span class="kn">from</span> <span class="n">sklearn.linear_model</span> <span class="kn">import</span> <span class="n">LogisticRegression</span>
<span class="kn">from</span> <span class="n">sklearn.neighbors</span> <span class="kn">import</span> <span class="n">KNeighborsClassifier</span>
<span class="kn">from</span> <span class="n">sklearn.tree</span> <span class="kn">import</span> <span class="n">DecisionTreeClassifier</span>

<span class="c1"># Create the individual classifiers
</span><span class="n">lr</span> <span class="o">=</span> <span class="nc">LogisticRegression</span><span class="p">(</span><span class="n">random_state</span><span class="o">=</span><span class="mi">42</span><span class="p">)</span>
<span class="n">knn</span> <span class="o">=</span> <span class="nc">KNeighborsClassifier</span><span class="p">()</span>
<span class="n">tree</span> <span class="o">=</span> <span class="nc">DecisionTreeClassifier</span><span class="p">(</span><span class="n">random_state</span><span class="o">=</span><span class="mi">42</span><span class="p">)</span>

<span class="c1"># Create the ensemble classifier
</span><span class="n">ensemble</span> <span class="o">=</span> <span class="nc">VotingClassifier</span><span class="p">(</span><span class="n">estimators</span><span class="o">=</span><span class="p">[(</span><span class="sh">'</span><span class="s">lr</span><span class="sh">'</span><span class="p">,</span> <span class="n">lr</span><span class="p">),</span> <span class="p">(</span><span class="sh">'</span><span class="s">knn</span><span class="sh">'</span><span class="p">,</span> <span class="n">knn</span><span class="p">),</span> <span class="p">(</span><span class="sh">'</span><span class="s">tree</span><span class="sh">'</span><span class="p">,</span> <span class="n">tree</span><span class="p">)],</span> <span class="n">voting</span><span class="o">=</span><span class="sh">'</span><span class="s">hard</span><span class="sh">'</span><span class="p">)</span>

<span class="c1"># Train the ensemble classifier
</span><span class="n">ensemble</span><span class="p">.</span><span class="nf">fit</span><span class="p">(</span><span class="n">X_train</span><span class="p">,</span> <span class="n">y_train</span><span class="p">)</span>

<span class="c1"># Make predictions on the test data
</span><span class="n">y_pred</span> <span class="o">=</span> <span class="n">ensemble</span><span class="p">.</span><span class="nf">predict</span><span class="p">(</span><span class="n">X_test</span><span class="p">)</span>
</code></pre></div></div> <p>In this example, we create a random forest classifier with 100 trees and use the square root of the number of features as the maximum number of features to consider at each split. We then train the classifier on the training data and make predictions on the test data.</p> <ul> <li><strong>Handling Imbalanced Data:</strong> Ensemble learning can also be useful when working with imbalanced data. Imbalanced data occurs when one class is much more prevalent than the others. This can make it difficult to train accurate models, as the model may be biased towards the majority class. Ensemble learning can help to address this issue by combining multiple models that have been trained on different subsets of the data or with different algorithms. This helps to reduce the bias towards the majority class and improve the accuracy of the model for all classes.</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">imblearn.ensemble</span> <span class="kn">import</span> <span class="n">BalancedBaggingClassifier</span>
<span class="kn">from</span> <span class="n">sklearn.tree</span> <span class="kn">import</span> <span class="n">DecisionTreeClassifier</span>

<span class="c1"># Create a decision tree classifier
</span><span class="n">tree</span> <span class="o">=</span> <span class="nc">DecisionTreeClassifier</span><span class="p">()</span>

<span class="c1"># Create a balanced bagging classifier
</span><span class="n">bag</span> <span class="o">=</span> <span class="nc">BalancedBaggingClassifier</span><span class="p">(</span><span class="n">base_estimator</span><span class="o">=</span><span class="n">tree</span><span class="p">,</span> <span class="n">n_estimators</span><span class="o">=</span><span class="mi">10</span><span class="p">,</span> <span class="n">random_state</span><span class="o">=</span><span class="mi">42</span><span class="p">)</span>

<span class="c1"># Train the balanced bagging classifier
</span><span class="n">bag</span><span class="p">.</span><span class="nf">fit</span><span class="p">(</span><span class="n">X_train</span><span class="p">,</span> <span class="n">y_train</span><span class="p">)</span>

<span class="c1"># Make predictions on the test data
</span><span class="n">y_pred</span> <span class="o">=</span> <span class="n">bag</span><span class="p">.</span><span class="nf">predict</span><span class="p">(</span><span class="n">X_test</span><span class="p">)</span>
</code></pre></div></div> <p>In this example, we create a decision tree classifier and use it as the base estimator for the balanced bagging classifier. We set the number of estimators to 10, which means we will train 10 models on different subsets of the data. The balanced bagging classifier uses a resampling strategy to balance the class distribution in each subset of the data.</p> <ul> <li><strong>Handling Large Datasets:</strong> Ensemble learning can also be useful when working with large datasets. Large datasets can be computationally expensive to train and may require specialized hardware or distributed computing systems. Ensemble learning can help to reduce the computational cost of training models by allowing us to train multiple models in parallel on different subsets of the data or with different algorithms.</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">sklearn.ensemble</span> <span class="kn">import</span> <span class="n">VotingClassifier</span>
<span class="kn">from</span> <span class="n">sklearn.linear_model</span> <span class="kn">import</span> <span class="n">LogisticRegression</span>
<span class="kn">from</span> <span class="n">sklearn.neighbors</span> <span class="kn">import</span> <span class="n">KNeighborsClassifier</span>
<span class="kn">from</span> <span class="n">sklearn.tree</span> <span class="kn">import</span> <span class="n">DecisionTreeClassifier</span>

<span class="c1"># Create the individual classifiers
</span><span class="kn">from</span> <span class="n">dask_ml.ensemble</span> <span class="kn">import</span> <span class="n">RandomForestClassifier</span>

<span class="c1"># Create a random forest classifier
</span><span class="n">rf</span> <span class="o">=</span> <span class="nc">RandomForestClassifier</span><span class="p">(</span><span class="n">n_estimators</span><span class="o">=</span><span class="mi">100</span><span class="p">,</span> <span class="n">max_features</span><span class="o">=</span><span class="sh">'</span><span class="s">sqrt</span><span class="sh">'</span><span class="p">,</span> <span class="n">random_state</span><span class="o">=</span><span class="mi">42</span><span class="p">)</span>

<span class="c1"># Train the random forest classifier using dask
</span><span class="n">rf</span><span class="p">.</span><span class="nf">fit</span><span class="p">(</span><span class="n">X_train_dask</span><span class="p">,</span> <span class="n">y_train_dask</span><span class="p">)</span>

<span class="c1"># Make predictions on the test data using dask
</span><span class="n">y_pred_dask</span> <span class="o">=</span> <span class="n">rf</span><span class="p">.</span><span class="nf">predict</span><span class="p">(</span><span class="n">X_test_dask</span><span class="p">)</span>
</code></pre></div></div> <p>In this example, we create a random forest classifier with 100 trees and use the square root of the number of features as the maximum number of features to consider at each split. We train the classifier using dask_ml, which is a library for distributed machine learning using Dask. This allows us to train the model on large datasets using multiple processors or even multiple machines.</p> <ul> <li><strong>Versatility:</strong> Ensemble methods can be applied to various machine learning tasks and with different types of models, making them a flexible tool.</li> </ul> <h2 id="types-of-ensemble-learning">Types of Ensemble Learning</h2> <p>There are different types of Ensemble Learning techniques which differ mainly by the type of models used (homogeneous or heterogeneous models), the data sampling (with or without replacement, k-fold, etc.) and the decision function (voting, average, meta model, etc). Therefore, Ensemble Learning techniques can be classified as:</p> <ul> <li><strong>Bagging(Bootstrap Aggregation)</strong> Bagging, or Bootstrap Aggregating, builds multiple models from bootstrapped subsets of the original data, then aggregates their predictions (typically by majority voting for classification or averaging for regression).</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">sklearn.datasets</span> <span class="kn">import</span> <span class="n">load_iris</span>
<span class="kn">from</span> <span class="n">sklearn.ensemble</span> <span class="kn">import</span> <span class="n">RandomForestClassifier</span>
<span class="kn">from</span> <span class="n">sklearn.model_selection</span> <span class="kn">import</span> <span class="n">train_test_split</span>
<span class="kn">from</span> <span class="n">sklearn.metrics</span> <span class="kn">import</span> <span class="n">accuracy_score</span>

<span class="n">iris</span> <span class="o">=</span> <span class="nf">load_iris</span><span class="p">()</span>
<span class="n">X</span><span class="p">,</span> <span class="n">y</span> <span class="o">=</span> <span class="n">iris</span><span class="p">.</span><span class="n">data</span><span class="p">,</span> <span class="n">iris</span><span class="p">.</span><span class="n">target</span>
<span class="n">X_train</span><span class="p">,</span> <span class="n">X_test</span><span class="p">,</span> <span class="n">y_train</span><span class="p">,</span> <span class="n">y_test</span> <span class="o">=</span> <span class="nf">train_test_split</span><span class="p">(</span><span class="n">X</span><span class="p">,</span> <span class="n">y</span><span class="p">,</span> <span class="n">test_size</span><span class="o">=</span><span class="mf">0.3</span><span class="p">,</span> <span class="n">random_state</span><span class="o">=</span><span class="mi">42</span><span class="p">)</span>

<span class="n">rf</span> <span class="o">=</span> <span class="nc">RandomForestClassifier</span><span class="p">(</span><span class="n">n_estimators</span><span class="o">=</span><span class="mi">100</span><span class="p">,</span> <span class="n">random_state</span><span class="o">=</span><span class="mi">42</span><span class="p">)</span>
<span class="n">rf</span><span class="p">.</span><span class="nf">fit</span><span class="p">(</span><span class="n">X_train</span><span class="p">,</span> <span class="n">y_train</span><span class="p">)</span>

<span class="n">y_pred</span> <span class="o">=</span> <span class="n">rf</span><span class="p">.</span><span class="nf">predict</span><span class="p">(</span><span class="n">X_test</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Random Forest Accuracy: </span><span class="si">{</span><span class="nf">accuracy_score</span><span class="p">(</span><span class="n">y_test</span><span class="p">,</span> <span class="n">y_pred</span><span class="p">)</span><span class="si">:</span><span class="p">.</span><span class="mi">2</span><span class="n">f</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <ul> <li><strong>Boosting</strong> <ul> <li><strong>Ada Boost</strong></li> <li><strong>Gradient Boosting</strong></li> <li><strong>XGBoost</strong> Boosting constructs a sequence of weak learners, each trying to correct the errors made by the previous one. The final prediction is obtained by weighted voting (classification) or weighted averaging (regression).</li> </ul> </li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">sklearn.ensemble</span> <span class="kn">import</span> <span class="n">AdaBoostClassifier</span>

<span class="n">adb</span> <span class="o">=</span> <span class="nc">AdaBoostClassifier</span><span class="p">(</span><span class="n">n_estimators</span><span class="o">=</span><span class="mi">100</span><span class="p">,</span> <span class="n">random_state</span><span class="o">=</span><span class="mi">42</span><span class="p">)</span>
<span class="n">adb</span><span class="p">.</span><span class="nf">fit</span><span class="p">(</span><span class="n">X_train</span><span class="p">,</span> <span class="n">y_train</span><span class="p">)</span>

<span class="n">y_pred_adb</span> <span class="o">=</span> <span class="n">adb</span><span class="p">.</span><span class="nf">predict</span><span class="p">(</span><span class="n">X_test</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">AdaBoost Accuracy: </span><span class="si">{</span><span class="nf">accuracy_score</span><span class="p">(</span><span class="n">y_test</span><span class="p">,</span> <span class="n">y_pred_adb</span><span class="p">)</span><span class="si">:</span><span class="p">.</span><span class="mi">2</span><span class="n">f</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <ul> <li><strong>Stacking</strong> Stacking involves training multiple diverse models and then training a meta-model to optimally combine the predictions of these base models.</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">sklearn.ensemble</span> <span class="kn">import</span> <span class="n">StackingClassifier</span>
<span class="kn">from</span> <span class="n">sklearn.tree</span> <span class="kn">import</span> <span class="n">DecisionTreeClassifier</span>
<span class="kn">from</span> <span class="n">sklearn.linear_model</span> <span class="kn">import</span> <span class="n">LogisticRegression</span>

<span class="n">estimators</span> <span class="o">=</span> <span class="p">[</span>
    <span class="p">(</span><span class="sh">'</span><span class="s">rf</span><span class="sh">'</span><span class="p">,</span> <span class="nc">RandomForestClassifier</span><span class="p">(</span><span class="n">n_estimators</span><span class="o">=</span><span class="mi">10</span><span class="p">,</span> <span class="n">random_state</span><span class="o">=</span><span class="mi">42</span><span class="p">)),</span>
    <span class="p">(</span><span class="sh">'</span><span class="s">adb</span><span class="sh">'</span><span class="p">,</span> <span class="nc">AdaBoostClassifier</span><span class="p">(</span><span class="n">n_estimators</span><span class="o">=</span><span class="mi">10</span><span class="p">,</span> <span class="n">random_state</span><span class="o">=</span><span class="mi">42</span><span class="p">)),</span>
    <span class="p">(</span><span class="sh">'</span><span class="s">dt</span><span class="sh">'</span><span class="p">,</span> <span class="nc">DecisionTreeClassifier</span><span class="p">(</span><span class="n">random_state</span><span class="o">=</span><span class="mi">42</span><span class="p">))</span>
<span class="p">]</span>
<span class="n">stacking_clf</span> <span class="o">=</span> <span class="nc">StackingClassifier</span><span class="p">(</span><span class="n">estimators</span><span class="o">=</span><span class="n">estimators</span><span class="p">,</span> <span class="n">final_estimator</span><span class="o">=</span><span class="nc">LogisticRegression</span><span class="p">())</span>
<span class="n">stacking_clf</span><span class="p">.</span><span class="nf">fit</span><span class="p">(</span><span class="n">X_train</span><span class="p">,</span> <span class="n">y_train</span><span class="p">)</span>

<span class="n">y_pred_stack</span> <span class="o">=</span> <span class="n">stacking_clf</span><span class="p">.</span><span class="nf">predict</span><span class="p">(</span><span class="n">X_test</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Stacking Accuracy: </span><span class="si">{</span><span class="nf">accuracy_score</span><span class="p">(</span><span class="n">y_test</span><span class="p">,</span> <span class="n">y_pred_stack</span><span class="p">)</span><span class="si">:</span><span class="p">.</span><span class="mi">2</span><span class="n">f</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <ul> <li><strong>Voting</strong> Voting methods directly combine predictions by majority voting (classification) or averaging (regression).</li> </ul> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">sklearn.ensemble</span> <span class="kn">import</span> <span class="n">VotingClassifier</span>

<span class="n">voting_clf</span> <span class="o">=</span> <span class="nc">VotingClassifier</span><span class="p">(</span><span class="n">estimators</span><span class="o">=</span><span class="n">estimators</span><span class="p">,</span> <span class="n">voting</span><span class="o">=</span><span class="sh">'</span><span class="s">hard</span><span class="sh">'</span><span class="p">)</span>
<span class="n">voting_clf</span><span class="p">.</span><span class="nf">fit</span><span class="p">(</span><span class="n">X_train</span><span class="p">,</span> <span class="n">y_train</span><span class="p">)</span>

<span class="n">y_pred_voting</span> <span class="o">=</span> <span class="n">voting_clf</span><span class="p">.</span><span class="nf">predict</span><span class="p">(</span><span class="n">X_test</span><span class="p">)</span>
<span class="nf">print</span><span class="p">(</span><span class="sa">f</span><span class="sh">"</span><span class="s">Voting Accuracy: </span><span class="si">{</span><span class="nf">accuracy_score</span><span class="p">(</span><span class="n">y_test</span><span class="p">,</span> <span class="n">y_pred_voting</span><span class="p">)</span><span class="si">:</span><span class="p">.</span><span class="mi">2</span><span class="n">f</span><span class="si">}</span><span class="sh">"</span><span class="p">)</span>
</code></pre></div></div> <ul> <li><strong>Other ensemble learning approaches</strong> <ul> <li><strong>Bucket of Models</strong></li> <li><strong>Bayesian Model Averaging</strong></li> </ul> </li> </ul> <h2 id="challenges-and-considerations">Challenges and Considerations</h2> <ul> <li><strong>Interpretability:</strong> Harder to interpret than single models.</li> <li><strong>Model Selection:</strong> Choosing base learners and ensemble strategy.</li> <li><strong>Computational Cost:</strong> Ensembles require more resources.</li> </ul> <h2 id="popular-algorithms-in-ensemble-learning">Popular Algorithms in Ensemble Learning</h2> <ul> <li> <p><strong>Random Forests:</strong> An ensemble of decision trees, typically trained via bagging, that introduces randomness in tree construction to further reduce correlation between individual trees.</p> </li> <li> <p><strong>AdaBoost:</strong> Iteratively reweights data samples, focusing more on previously misclassified instances. Each learner’s contribution to the final model is weighted based on accuracy.</p> </li> <li> <p><strong>Gradient Boosting:</strong> Constructs models sequentially by fitting the residuals of previous models. Widely used due to high predictive accuracy and flexibility.</p> </li> <li> <p><strong>XGBoost and LightGBM:</strong> Optimized gradient boosting algorithms designed for speed and scalability, popular for structured/tabular data.</p> </li> </ul> <h2 id="applications-of-ensemble-learning">Applications of Ensemble Learning</h2> <ul> <li><strong>Finance:</strong> Risk assessment, stock price prediction</li> <li><strong>Healthcare:</strong> Disease diagnosis, patient risk scoring</li> <li><strong>Marketing:</strong> Customer segmentation, churn prediction</li> </ul> <h2 id="current-research-progress">Current Research Progress</h2> <p>Recent research has focused on improving ensemble methods through better aggregation strategies, deeper integration with neural network architectures (deep ensembles), and optimizing computational efficiency through distributed computing frameworks. Techniques such as Bayesian ensembles and reinforcement learning-based stacking methods have emerged to further enhance model robustness and accuracy.</p> <h2 id="future-research-trends-and-directions">Future Research Trends and Directions</h2> <p>Future research in ensemble learning may include:</p> <ul> <li><strong>Automated Machine Learning (AutoML):</strong> Developing automated frameworks for ensemble selection and hyperparameter tuning.</li> <li><strong>Interpretable Ensembles:</strong> Enhancing interpretability and explainability of ensemble models.</li> <li><strong>Robustness and Fairness:</strong> Creating ensembles that are robust against adversarial attacks and biases.</li> <li><strong>Hybrid Models:</strong> Combining ensemble methods with emerging techniques like deep learning, transfer learning, and federated learning.</li> </ul> <h2 id="conclusion">Conclusion</h2> <p>Ensemble learning is a foundational technique in machine learning, substantially enhancing predictive performance. Understanding its methods, algorithms, and practical considerations is crucial for applying ensembles effectively to real-world problems.</p> <script src="https://giscus.app/client.js" data-repo="WenbinWang12/WenbinWang12.github.io" data-repo-id="R_kgDON6rj4w" data-category="Announcements" data-category-id="DIC_kwDON6rj484CsCS7" data-mapping="pathname" data-strict="0" data-reactions-enabled="1" data-emit-metadata="0" data-input-position="bottom" data-theme="preferred_color_scheme" data-lang="en" crossorigin="anonymous" async=""> </script>]]></content><author><name>Wenbin Wang</name></author><category term="distill"/><category term="formatting"/><summary type="html"><![CDATA[“Two heads are better than one.”]]></summary></entry></feed>