<div class="news">
  {% assign current_lang = page.lang | default: site.lang | default: 'en' %}
  {% if current_lang == 'zh' %}
    {% assign news_date_format = '%Y年%m月%d日' %}
    {% assign no_news_text = '暂无新闻。' %}
  {% else %}
    {% assign news_date_format = '%b %d, %Y' %}
    {% assign no_news_text = 'No news so far...' %}
  {% endif %}
  {% if site.news != blank %}
    {% assign news_size = site.news | size %}
    <div
      class="table-responsive"
      {% if include.limit and page.announcements.scrollable and news_size > 3 %}
        style="max-height: 60vw"
      {% endif %}
    >
      <table class="table table-sm table-borderless">
        {% assign news = site.news | reverse %}
        {% if include.limit and page.announcements.limit %}
          {% assign news_limit = page.announcements.limit %}
        {% else %}
          {% assign news_limit = news_size %}
        {% endif %}
        {% for item in news limit: news_limit %}
          <tr>
            <th scope="row" style="width: 20%">{{ item.date | date: news_date_format }}</th>
            <td>
              {% if item.inline %}
                {{ item.content | remove: '<p>' | remove: '</p>' | emojify }}
              {% else %}
                <a class="news-title" href="{{ item.url | relative_url }}">{{ item.title }}</a>
              {% endif %}
            </td>
          </tr>
        {% endfor %}
      </table>
    </div>
  {% else %}
    <p>{{ no_news_text }}</p>
  {% endif %}
</div>
